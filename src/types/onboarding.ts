export interface OnboardingFormValues {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  corpNumber: string;
}

export interface SubmitPayload {
  firstName: string;
  lastName: string;
  phone: string;
  corporationNumber: string;
}
