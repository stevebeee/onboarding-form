import { CSSProperties } from 'react';

export const containerStyle: CSSProperties = {
  maxWidth: 600,
  margin: '100px auto',
  padding: '20px',
};

export const nameRowStyle: CSSProperties = {
  display: 'flex',
  gap: '20px',
};

export const nameItemStyle: CSSProperties = {
  flex: 1,
};
